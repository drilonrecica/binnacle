# documentation: https://github.com/drilonrecica/talos
# slogan: Lightweight, read-only monitoring for Coolify, Docker, and Linux hosts.
# category: monitoring
# tags: monitoring,docker,coolify,host-metrics
# port: 8080

services:
  talos:
    image: ghcr.io/drilonrecica/talos:stable
    container_name: talos
    restart: unless-stopped
    read_only: true
    privileged: false
    user: "talos:${DOCKER_GID:?DOCKER_GID is required; run export DOCKER_GID=$(getent group docker | cut -d: -f3)}"
    security_opt:
      - no-new-privileges:true
    environment:
      TALOS_DATA_DIR: /var/lib/talos
      TALOS_HOST_PROC: /host/proc
      TALOS_HOST_SYS: /host/sys
      TALOS_SETUP_TOKEN: ${TALOS_SETUP_TOKEN:?TALOS_SETUP_TOKEN is required for the first installation}
    volumes:
      - talos-data:/var/lib/talos
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /etc/os-release:/host/etc/os-release:ro
      - /var/run/docker.sock:/var/run/docker.sock
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/127.0.0.1:8080 && printf 'GET /api/v1/session HTTP/1.1\\r\\nHost: 127.0.0.1\\r\\nConnection: close\\r\\n\\r\\n' >&3 && head -1 <&3 | grep -q HTTP/1.1"]
      interval: 30s
      timeout: 5s
      start_period: 5s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 128M
        reservations:
          memory: 64M

volumes:
  talos-data:
